name: ftnl_ui
description: Host-controlled File Tunnel picker for Flutter on mobile, desktop, and web.
version: 0.1.0
repository: https://github.com/file-tunnel/ftnl-ui-components
environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.44.0"
dependencies:
  flutter:
    sdk: flutter
  qr_flutter: ^4.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
