# Security policy

Report vulnerabilities using GitHub Security Advisories. Pairing URIs and
capabilities are credentials; never include them in issues, snapshots, preview
fixtures, telemetry, or accessibility labels.

QR views should set platform screenshot/recents protections when the host's
threat model requires them. Components must not copy pairing URIs to the
clipboard unless the user explicitly chooses a host-provided fallback.
